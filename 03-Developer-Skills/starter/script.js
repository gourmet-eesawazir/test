"use strict";

/*
// const input = [17, 21, 23];
const input = [12, 5, -5, 0, 4];

const printForecast = input => {
    let outputStr = "...";
    for (let i = 0; i < input.length; i++) {
        outputStr += " " + input[i] + "°C in " + (i + 1) + " day" + (i > 0 ? "s" : "") + " ...";
    }
    return outputStr;
}

console.log(printForecast(input));
*/
